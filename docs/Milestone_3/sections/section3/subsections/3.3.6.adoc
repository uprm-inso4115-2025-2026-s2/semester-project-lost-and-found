
==== *Stakeholder Documentation and Justification*

[discrete]
==== Example User Scenarios for the Lost & Found System

This section describes example user interaction scenarios for the Lost & Found system. These scenarios illustrate common behaviors within the platform, including reporting lost items, posting found items, submitting claims, and reviewing claims.

===== Introduction

The Lost & Found system supports multiple types of user interactions related to reporting, searching, and recovering items. The following scenarios present common flows that help define how users and administrators are expected to interact with the system.

image::use-case-diagram.png[Use Case Diagram, align=center, pdfwidth=80%]
_Use case diagram describing interactions between actors and the platform_

===== Scenario 1: Reporting a Lost Item

*Actor:* User

. A user logs into the Lost & Found platform.
. The user selects "Report Lost Item."
. The user enters item details such as description, location, and date lost.
. The system saves the lost item report and makes it searchable.

*Result:* The lost item is stored in the system and can later be matched with found items.

===== Scenario 2: Posting a Found Item

*Actor:* Finder (User)

. A user finds an item on campus.
. The user logs into the platform.
. The user selects "Post Found Item."
. The user enters details about the item and where it was found.
. The system publishes the found item listing.

*Result:* The found item becomes visible to users searching for lost items.

===== Scenario 3: Submitting an Item Claim

*Actor:* User

. A user searches the platform for a lost item.
. The user finds a listing that matches their item.
. The user submits a claim request.
. The system records the claim and sends it for review.

*Result:* The claim is stored in the system and awaits verification.

===== Scenario 4: Admin Reviews a Claim

*Actor:* Administrator

. The administrator reviews submitted claim requests.
. The administrator verifies the claim information.
. The administrator decides whether the claim is valid.
. The claim is approved or rejected.

*Result:* If approved, the item can be returned to the owner.

===== Scenario 5: User Creates an Account

*Actor:* User

. The user enters account details at the sign-up page.
 .. If the email is not a UPR email address, an error is shown. The user remains in the sign-up page.
 .. If the email is a UPR email address, it is validated and the account is created.
. The user enters the newly-created account credentials at the login page.
 .. If the account credentials are valid/correct, the user is logged in successfully.
 .. If the account credentials are not valid/correct, the user is shown an error message and remains in the login page. 

*Result:* User has an account and access to the platform's features.

image::user_account_creation_event_trace_diagram.png[User Account Creation Event Trace Diagram, align=center, pdfwidth=50%]
_User Account Creation Event Trace Diagram_

===== Scenario Validation Considerations

The following validation requirements and edge cases should be considered when implementing the documented workflows.

====== Reporting a Lost Item

Preconditions:

* User is authenticated.
* Required item information is available.
* The report contains a valid date and description.

Potential failure cases:

* Missing required fields.
* Invalid date values.
* Duplicate reports for the same item.
* Network interruption during submission.

====== Posting a Found Item

Preconditions:

* User is authenticated.
* Item details can be reasonably described.

Potential failure cases:

* Missing location information.
* Incomplete item descriptions.
* Invalid submission data.
* Duplicate found-item listings.

====== Submitting an Item Claim

Preconditions:

* Matching item listing exists.
* User is authenticated.

Potential failure cases:

* Claim submitted for an unrelated item.
* Missing ownership evidence.
* Duplicate claim requests.
* Item already returned to another user.

====== Reviewing a Claim

Preconditions:

* Administrator privileges are available.
* Claim information has been submitted.

Potential failure cases:

* Insufficient ownership evidence.
* Incomplete claim information.
* Conflicting claim requests.
* Administrative processing errors.

====== User Account Creation

Preconditions:

* User has access to a valid UPR email address.
* Required registration information is available.

Potential failure cases:

* Invalid UPR email address.
* Email already registered.
* Missing required account information.
* Authentication service unavailable.

====== Expected Validation Outcomes

The system should reject incomplete submissions, notify users when validation fails, and preserve data consistency throughout all item lifecycle operations.
